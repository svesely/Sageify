<h1>Assessment: <%= @exam.name %></h1>

<%= form_for @assessment do |f| %>
  <ol>
    <%= render :partial => 'question', :collection => @exam.questions, :locals => {:f => f }%>
  </ol>
  <%= f._field :exam_id %>
  <%= f.submit "Submit", :disable_with => 'Submiting...' %>
<% end -%>
