<% html_title("#{@page.lesson.name}: #{@page.title}") %>
<h2><%= h @page.title %></h2>
<div id="lesson_page_content">
  <%= safe_textile(@page.content) %>
</div>
<div id="page_controls">
  <%= page_controls_for_page(@page) %>
</div>

<%= exam_link_list(@page) %>